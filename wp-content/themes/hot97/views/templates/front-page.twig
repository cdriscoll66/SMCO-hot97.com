{% extends "base.twig" %}

{% block content %}
    <main id="main" class="entry-content">
        <h1>{{ title }}</h1>

        {# Hero #}
        {% if hero %}
            {% for post in hero %}
                {% include "components/molecules/hero.twig" %}
            {% endfor %}
        {% endif %}

        {# Featured Categories #}
        {% if featured %}
            {% for group in featured %}
                <h2>{{ group.term.name }}</h2>

                {% if group.posts %}
                    <ul class="posts">
                        {% for post in group.posts %}
                            <li class="posts__item">
                                {% include "components/molecules/card.twig" %}
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            {% endfor %}
        {% endif %}

        {# DJs #}
        {% include "components/organisms/dj-carousel.twig" %}


        {# Marketing Content #}
        {{ content|raw }}

        {# Other Categories #}
        {% if other %}
            {% for group in other %}
                <h2>{{ group.term.name }}</h2>
                <a href="{{ group.term.path }}">View All {{ group.term.name }}</a>

                {% if group.posts %}
                    <ul class="posts">
                        {% for post in group.posts %}
                            <li class="posts__item">
                                {% include "components/molecules/card.twig" %}
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            {% endfor %}
        {% endif %}
    </main>
    {% include "components/organisms/prefooter-home.twig" %}
{% endblock %}
